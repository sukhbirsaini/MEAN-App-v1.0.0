import { LastSearchPipe } from './last-search.pipe';

describe('LastSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LastSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
